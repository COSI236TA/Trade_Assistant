<%= form_for(@rule) do |f| %>

  <% if @rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rule.errors.count, "error") %> prohibited this rule from being saved:</h2>

      <ul>
      <% @rule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li> 
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field" >
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>

    <div class="field" >
      <%= f.label :description %>
      <%= f.text_field :description %>
    <div>

    <div class="field" >
      <%= f.label :property %>
      <%= f.text_field :property %>
    </div>

    <div class="field" >
      <%= f.label :rel %>
      <%= f.text_field :rel %>
    </div>

    <div class="field" >
      <%= f.label :target %>
      <%= f.text_field :target %>
    </div>

    <div class="field" >
      <%= f.label :activated %>
      <%= f.text_field :activated %>
    </div>

    <div class="field" >
      <%= f.label :notification_method %>
      <%= f.text_field :notification_method %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
    
<% end %>